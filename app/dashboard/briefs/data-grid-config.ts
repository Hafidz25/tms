export const statusOption = [
    "Assigned",
    "In Review",
    "Waiting for Client",
    "Correction",
    "In Progress",
    "Need Review",
    "Done",
  ].map((s) => ({
    value: s,
    label: s,
  }));